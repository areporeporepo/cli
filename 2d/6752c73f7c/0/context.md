# Session Context

## User Prompts

### Prompt 1

Implement the following plan:

# Add OpenCode to Agent Implementation Guide

## Context

The agent guide (`docs/architecture/agent-guide.md`) documents how to integrate new agents with the Entire CLI. It currently references Claude Code, Gemini CLI, and Cursor IDE in its examples, tables, and test references — but omits OpenCode, which is a fully implemented agent with 10 source files and some unique patterns (TypeScript plugin, CLI-based transcript export, JSON format). This creates an incom...

