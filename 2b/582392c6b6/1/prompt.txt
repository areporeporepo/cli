Implement the following plan:

# Plan: Restructure E2E triage/debug into a plugin

## Context

The `e2e-triage` and `debug-e2e` skills are standalone skills that should be restructured into a plugin (like `agent-integration`) with clear command separation: triage-ci (report), debug (deep-dive), and implement (apply fixes + verify).

## Current Structure (to be deleted)

```
.claude/skills/e2e-triage/SKILL.md
.claude/skills/e2e-triage/README.md
.claude/skills/debug-e2e/SKILL.md
```

## Target Structure

```
.claude/plugins/e2e/
├── .claude-plugin/
│   └── plugin.json
├── README.md
└── commands/
    ├── triage-ci.md          # /e2e:triage-ci
    ├── debug.md              # /e2e:debug
    └── implement.md          # /e2e:implement

.claude/skills/e2e/
├── SKILL.md                  # Orchestrator: triage-ci → implement
├── triage-ci.md              # Full triage procedure
├── debug.md                  # Full debug procedure
└── implement.md              # Full implement procedure
```

## Commands

### `/e2e` (orchestrator skill — SKILL.md)
Runs triage-ci then implement sequentially. Accepts: test names, `--agent`, artifact path, CI run ref.

### `/e2e:triage-ci` (plugin command)
- **Input:** test names, `--agent`, artifact path, or CI run reference (`latest`, run ID, run URL)
- **Does:** Downloads CI artifacts, re-runs failing tests locally (up to 3x), analyzes artifacts, classifies failures (flaky/real-bug/test-bug), correlates across agents
- **Output:** Findings report with classifications + summary table
- **Does NOT apply fixes** — report only
- **Content from:** current `e2e-triage/SKILL.md` Steps L1-L5 + Shared Analysis Steps 1-3 + Step 4 "Present Findings Report" only + Step 5 summary (no actions)

### `/e2e:debug` (plugin command)
- **Input:** artifact path (local or from triage-ci)
- **Does:** Deep-dive analysis of specific test failure artifacts (console.log → test source → entire.log → repo state)
- **Output:** Root cause analysis
- **Content from:** current `debug-e2e/SKILL.md` (moved as-is)

### `/e2e:implement` (plugin command)
- **Input:** Findings from `/e2e:triage-ci` or `/e2e:debug` (in conversation context), or standalone with known failure descriptions
- **Does:** Proposes fixes, asks user to approve, applies fixes, runs scoped E2E verification
- **Output:** Applied fixes + verification results + summary table
- **Content from:** current `e2e-triage/SKILL.md` Step 4 "For flaky/real-bug" sections + scoped E2E verification + Step 5 summary with "Action Taken"

## Content Split Detail

### `triage-ci.md` — from `e2e-triage/SKILL.md`
- Step L1: Parse User Input (CI artifact download + cost warning)
- Steps L2-L5: First run, re-run, tiebreaker, collect results
- Step 1: Analyze Each Failure (delegates to debug procedure)
- Step 2: Classify Each Failure (re-run signal table, real-bug/flaky signals)
- Step 3: Cross-Agent Correlation
- Present Findings Report (report blocks per test+agent)
- Summary table (classification only, no "Action Taken")

### `debug.md` — from `debug-e2e/SKILL.md`
- Move entire content as-is

### `implement.md` — new, extracted from `e2e-triage/SKILL.md`
- Accept findings from triage-ci or debug (or standalone description)
- For flaky failures: describe proposed fix
- For real-bug failures: describe root cause analysis
- Ask user which fixes to apply
- Apply fixes in working tree
- Run static checks (fmt, lint, canary)
- Run scoped real E2E verification (agent-specific / shared / prompt-only)
- Summary table with "Action Taken"

## Files to Create (9 files)
1. `.claude/plugins/e2e/.claude-plugin/plugin.json`
2. `.claude/plugins/e2e/README.md`
3. `.claude/plugins/e2e/commands/triage-ci.md`
4. `.claude/plugins/e2e/commands/debug.md`
5. `.claude/plugins/e2e/commands/implement.md`
6. `.claude/skills/e2e/SKILL.md`
7. `.claude/skills/e2e/triage-ci.md`
8. `.claude/skills/e2e/debug.md`
9. `.claude/skills/e2e/implement.md`

## Files to Delete (3 files)
- `.claude/skills/e2e-triage/SKILL.md`
- `.claude/skills/e2e-triage/README.md`
- `.claude/skills/debug-e2e/SKILL.md`

## References to Update
- Check CLAUDE.md for references to old skill names (`e2e-triage`, `debug-e2e`)
- Check other skills that reference `debug-e2e` (triage-ci.md references debug procedure)

## Verification
1. Confirm plugin.json is valid (matches agent-integration pattern)
2. Confirm old skill directories are empty/removed
3. Grep for stale references to `e2e-triage` or `debug-e2e` across `.claude/`


If you need specific details from before exiting plan mode (like exact code snippets, error messages, or content you generated), read the full transcript at: /Users/alisha/.claude/projects/-Users-alisha-Projects-devenv-cli/00468b34-359e-43a2-b0bb-30677f1aee5e.jsonl

---

commit