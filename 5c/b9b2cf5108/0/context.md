# Session Context

## User Prompts

### Prompt 1

Implement the following plan:

# Plan: Add Architecture Doc References to Agent Integration Skill

## Context

The agent integration skill (`/agent-integration`) has three commands (probe, e2e-tests, implement) that guide building new agent integrations. The sub-prompt files already reference `docs/architecture/agent-guide.md` and `docs/architecture/agent-integration-checklist.md` as "required reading" steps, but the main `SKILL.md` file doesn't mention them. Adding them to SKILL.md ensures t...

